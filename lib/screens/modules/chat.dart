
import 'package:flutter/material.dart';

class chatscrean extends StatelessWidget {
  const chatscrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('chat screan'),
    );
  }
}
