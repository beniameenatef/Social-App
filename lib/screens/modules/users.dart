
import 'package:flutter/material.dart';

class usersscrean extends StatelessWidget {
  const usersscrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('users screan'),
    );
  }
}
